extends index.pug

block content
    h1(align="center") MOMENTUM
    .cards
        a.card(href=`/member/feed/` + username) Feed
        a.card(href=`/community`) Communities
        a.card(href=`/member/`) Members
        a.card(href=`/about-us`) About Momentum
