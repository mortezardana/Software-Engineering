doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Momentum
    link(rel="stylesheet", href="/styles.css")
    
    script.
      function goToProfile() {
        let username = "john_doe"; // Replace this dynamically if needed
        window.location.href = "/member/" + username;
      }

  body
    header
      h1 Momentum
      div
        button.menu-toggle#menuToggle ☰
        button.post-btn#postBtn Post
        button.account-btn#accountBtn(onclick="goToProfile()") 👤

    nav.sidebar#sidebar
      ul
        li
          a(href="/feed/john_doe") Feed
        li
          a(href="#") Communities

    div.content
      block content
